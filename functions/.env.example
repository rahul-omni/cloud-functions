# Param for supremeCourtOTF only. Other cloud functions still use functions.config().
# OpenAI key is read from Secret Manager (see SECRET_MANAGER_SETUP.md).
#
# phhcUpsert, highCourtCasesUpsert, delhiDistrictCourtUpsert: use Secret Manager V2 (production) + .env (local/emulator).
# - Production: add secret DATABASE_URL in Secret Manager (PostgreSQL connection string).
# - Local/emulator: set DATABASE_URL below (same value as in Secret Manager).

DATABASE_URL=
